<% content_for :locale_links do  %>
  <p>
    <%= link_to_unless I18n.locale == :en, t('app.language.english'), new_event_path(locale: :en) %>
    |
    <%= link_to_unless I18n.locale == :ru, t('app.language.russian'), new_event_path(locale: :ru) %>
  </p>
<% end %>

<div class="row">
  <div class="col-md-6">
    <%= render 'form' %>
  </div>
</div>